let numeros = [2,4,6,8,10]
let multiplicador = []
//criar outra lista que multiplica por 2 com array
for (let index = 0; index < numeros.length; index++) { 
    multiplicador[index] = numeros [index] * 2
    // multiplicador.push = ( numeros * 2);
} 

console.log(multiplicador)